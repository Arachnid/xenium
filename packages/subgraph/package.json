{
  "name": "@xenium-eth/subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service arachnid/xeniumRinkeby",
    "create-local": "graph create --node http://localhost:8020/ xenium",
    "remove-local": "graph remove --node http://localhost:8020/ xenium",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xenium"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
